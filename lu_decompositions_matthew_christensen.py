# -*- coding: utf-8 -*-
"""LU_decompositions-Matthew_Christensen.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1AHjJVgCvCrrfoczHWr-ZyDXWP9gbCMgh

#**Lab 5 - LU decompositions and Gaussian elimination**

Enter your code in the spaces provided. Do not change any of the variable names or function names that are already provided for you. In places where we specify the name of the return value, make sure that your code produces the a value with the correct name.
"""

# Do not edit this cell.

LabID="Lab5"

try:
  from graderHelp import ISGRADEPLOT
except ImportError:
  ISGRADEPLOT = True

"""**Enter your name, section number, and BYU NetID**"""

# Enter your first and last names in between the quotation marks.

first_name="Matthew"

last_name="Christensen"

# Enter your Math 215 section number in between the quotation marks. 

section_number="001"  

# Enter your BYU NetID in between the quotation marks.  NOT YOUR BYU ID NUMBER! 

BYUNetID="mrc621"

"""**Import NumPy**"""

import numpy as np

"""**Problem 1**"""

def augment(A,b):  
  Ab=np.column_stack((A,b))
  return Ab # Put your return value here.

A=np.array([[1,1,1],[1,4,2],[4,7,8]])
b=np.array([1,3,9])

augment(A,b)

"""**Problem 2**"""

def first_column_zeros(A):
  B=np.copy(A)
  
  for row in range (1,B.shape[0]):
    cof = B[row,0] / B[0,0]
    B[row,:] -= cof*B[0,:]
  return B

A=np.array([[2,1,3,1],[1,2,-1,2.5],[4,2,-1,1]])
first_column_zeros(A)

"""**Problem 3**"""

def row_echelon(A,b): 
  B = np.copy(A)
  C = augment(B,b)

  for col in range(0,C.shape[1]-1):
    print("Col: " + str(col))
    for row in range(col+1,C.shape[0]):
      print("Row: " + str(row))
      cof = C[row,col] / C[col,col]
      C[row,:] -= cof*C[col,:]

  return C

A=np.array([[3.,1.,-2.],[1.,2.,-5.],[2.,-4.,1.]])
b=np.array([1.1,2,-3])
row_echelon(A,b)

"""**Problem 4**"""

def LU_decomposition(A):
  # Put your code here.

  m, n = A.shape
  U=np.copy(A)
  L=np.identity(len(A))

  for j in range(0,n):
    print("j: " + str(j))
    for i in range(j+1,m):
      print("\ti: " + str(i))
      L[i,j] = U[i,j] / U[j,j]
      U[i,:] = U[i,:] - (L[i,j] * U[j,:]) 
  
  print("We finished")
  return L,U # We've included the return values for you, though your function needs to define them correctly.

A=np.array([[3,1,-2],[1.5,2,-5],[2,-4,1]])
LU_decomposition(A)

"""**Problem 5**"""

def forward_substitution(L,b): # Accepts a lower triangular square matrix L and a vector b, solves Ly=b for y.
  n = len(b)
  print(n)
  y = np.zeros(n)
  print(y)
  for i in range(0,n):
    y[i] = (b[i] - np.dot(y, L[i,:]))/L[i,i]
  
  return y

L=np.array([[1,0,0],[3,1,0],[-1.1,2,1]])
b=np.array([-2.1,1,-1])
forward_substitution(L,b)

"""**Problem 6**"""

def back_substitution(L,b):    # Accepts an upper triangular square matrix U and a vector b, solves Ux=b for x.
  print("Start")
  n = len(b)
  print(n)
  y = np.zeros(n)
  print(y)
  for i in list(reversed(range(0,n))):
    print(i)
    y[i] = (b[i] - np.dot(y, L[i,:]))/L[i,i]
  
  return y

L=np.array([[2,-3.1,1],[0,1,3],[0,0,4]])
b=np.array([1,-2.1,3])
back_substitution(L,b)

"""**Problem 7**"""

def LU_solver(A,b): 
  L, U = LU_decomposition(A)
  print("L: " + str(L) + "U: " + str(U))
  y = forward_substitution(L,b)
  x = back_substitution(U,y)
  return x

A=np.array([[3,1,-2],[1.5,2,-5],[2,-4,1]])
b=np.array([1.1,3,-2])
LU_solver(A,b)

"""**STOP!  BEFORE YOU SUBMIT THIS LAB:**  Go to the "Runtime" menu at the top of this page, and select "Restart and run all".  If any of the cells produce error messages, you will either need to fix the error(s) or delete the code that is causing the error(s).  Then use "Restart and run all" again to see if there are any new errors.  Repeat this until no new error messages show up.

**You are not ready to submit until you are able to select "Restart and run all" without any new error messages showing up.  Your code will not be able to be graded if there are any error messages.**

To submit your lab for grading you must first download it to your compute as .py file.  In the "File" menu select "Download .py".  The resulting file can then be uploaded to https://linearalgebra.byu.edu for grading.
"""